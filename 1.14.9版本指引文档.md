# Typora 激活验证绕过 —— 指引文档

## 0. 前言

本文的思路是：

先完整还原激活验证的信任链；
找到信任链中所有判定收敛的单点；
用最少、语义最明确的补丁切断信任链，并处理容易被忽略的服务器心跳反噬。
最终效果：永久"已激活"状态，无视试用到期、无视激活服务器复核，编辑器功能完整无水印。

## 1. 目标概况与侦察

```
$ codesign -dv /Applications/Typora.app
Identifier=abnerworks.Typora
Format=app bundle with Mach-O universal (x86_64 arm64)
flags=0x10000(runtime)   # 带 Hardened Runtime
TeamIdentifier=9HWK5273G4
```
程序结构是"原生壳 + Web 内核"：

```
Typora.app/Contents/
├── MacOS/Typora            # 3.1MB 原生通用二进制（ObjC）★ 激活逻辑在这里
├── Frameworks/             # Sparkle(自动更新)、Sentry(崩溃上报)
└── Resources/TypeMark/     # 编辑器 Web 前端（HTML/JS，WKWebView 加载）
```
通过 otool -L 确认二进制只链接了 Cocoa/WebKit 等系统库，没有内嵌 V8/Node——TypeMark/atom/ 下的 V8 字节码（atom.compiled.dist.jsc）是 Windows/Linux(Electron) 版共用的资源，macOS 版不会执行。因此激活验证一定在原生层，渲染层 JS 只负责 UI（水印、弹窗），这是后续所有分析的前提。

## 2. 定位激活逻辑

对主二进制做字符串和符号侦察：

```
$ strings -n 6 Typora | grep -iE "license|activat|trial"
LicenseManager
LicenseWindowController
api/client/activate
api/client/deactivate
...
$ nm -m Typora | grep LicenseManager
```
得到核心类 LicenseManager 的完整方法列表（符号未 stripping，逆向难度大幅降低）：

```
+[LicenseManager sharedInstance]
-[LicenseManager start]              ← 启动入口
-[LicenseManager readLicenseInfo]    ← 读本地许可证
-[LicenseManager _readLicenseInfo:]
-[LicenseManager hasLicense]         ← ★ 判定取值口
-[LicenseManager cannotContonueUse:] ← ★ 试用到期闸门（官方代码就有这个拼写错误）
-[LicenseManager writeLicenseInfo:with:from:]
-[LicenseManager verifySig:]         ← RSA 验签
-[LicenseManager renew]              ← 联网心跳复核
-[LicenseManager unfillLicense]      ← 心跳失败后的撤销
-[LicenseManager recordFilePathNew/Old]  ← 许可证文件路径
...
```
另有工具类 Crypto：encryptAES: / decryptAES: / rawHash: / verify:with:（字符串里能直接看到内嵌的 -----BEGIN PUBLIC KEY-----）。

反汇编工具用系统自带的 lldb 批处理模式即可（无需 IDA/Hopper），它能自动注解 objc_msgSend 的 selector：

```
lldb -b -o "target create --arch arm64 /Applications/Typora.app/Contents/MacOS/Typora" \
     -o "disassemble --name '-[LicenseManager start]'" -o quit
```

## 3. 还原激活验证信任链

逐个方法读完反汇编后，可以还原出完整机制：

┌──────────────────────────── 启动路径 ────────────────────────────┐
│ App 启动 → -[LicenseManager start]                               │
│   ├─ readLicenseInfo                                             │
│   │    └─ _readLicenseInfo: 读 ~/Library/Application Support/    │
│   │       abnerworks.Typora/.T9WXXJvbUM                          │
│   │         ① NSData dataWithContentsOfFile:                     │
│   │         ② Crypto decryptAES:  (AES-256-CBC, IV=0)            │
│   │         ③ NSKeyedUnarchiver 解出 NSDictionary                │
│   │    → 字典中同时存在 "email" 和 "license" 两个 key            │
│   │      则 _hasLicense = YES   ⚠️ 全程不验 RSA 签名！           │
│   └─ renew → 每 12h POST api/client/renew                        │
│              ├─ 服务器 success=YES → 重写许可证                  │
│              ├─ 服务器 success=NO  → unfillLicense 撤销 ⚠️       │
│              └─ 网络失败 → failedCounts++（宽限）                │
└──────────────────────────────────────────────────────────────────┘
┌──────────────────────────── 执行路径 ────────────────────────────┐
│ -[Document makeWindowControllers]  (打开文档窗口)                │
│ -[TyWindowController newTab:]      (新建标签页)                  │
│   └─ cannotContonueUse:                                          │
│        ├─ hasLicense == YES → 放行                               │
│        └─ NO 且 dayRemains < 1（20 天试用到期）                  │
│           → 强制弹 LicenseWindowController 激活面板              │
│ JS 渲染层：File.option.hasLicense 控制水印（只是 UI，非闸门）    │
└──────────────────────────────────────────────────────────────────┘

几个关键细节：

### 3.1 本地许可证的加密

key = SHA256( IOPlatformUUID + "typora-license" )      # 32 字节
密文 = AES-256-CBC(key, IV=0, PKCS7) of NSKeyedArchiver(字典)
CCCrypt 的 options=1（只有 kCCOptionPKCS7Padding，没有 kCCOptionECBMode），所以是 CBC 而非 ECB——直接拿 ECB 解会看到"第一个块是 bplist00、后面全是乱码"的典型现象，这也是判断 CBC+零 IV 的实用技巧。用一行命令即可解密本机许可证：

```
UUID=$(ioreg -rd1 -c IOPlatformExpertDevice | awk -F'"' '/IOPlatformUUID/{print $4}')
KEY=$(python3 -c "import hashlib;print(hashlib.sha256(('$UUID'+'typora-license').encode()).hexdigest())")
openssl enc -d -aes-256-cbc -K "$KEY" -iv 0 \
  -in ~/Library/Application\ Support/abnerworks.Typora/.T9WXXJvbUM | plutil -p -
```

### 3.2 RSA 验签只发生在"写入"时

全二进制交叉引用确认：verifySig: 只有 writeLicenseInfo:with:from: 一个调用者。即——服务器下发/离线激活的许可证在写入时验签；而启动读取时完全不验签。这是设计缺陷：本地文件的真实性仅靠 AES 保密性保护，而 AES 密钥本机可推导（见 3.1），等于没有保护。

### 3.3 hasLicense 是所有判定的收敛点

-[LicenseManager hasLicense]:
    ldr  x0, [x0, #0x18]        ; 取 ivar _hasLicense
    cbz  x0, return_yes         ; nil 时竟然默认 YES（fail-open）
    b    _objc_msgSend$boolValue
return_yes:
    mov  w0, #0x1
    ret

cannotContonueUse:、showLicenseIfNeeded、给 JS 层的查询接口，全部经由这一个 getter。这是典型的单点失效。

### 3.4 容易被忽略的心跳反噬

renew 的回调里：服务器明确返回 success=NO 时调用 unfillLicense——清空本地许可证、置 _hasLicense=NO、并 postNotification 通知 JS 重新显示水印。也就是说：只补 hasLicense 的半成品补丁，会在 12 小时内被服务器"远程撤销"，补丁时必须把 unfillLicense 一起处理。

### 3.5 DYLD 注入这条路为什么不通

entitlement 里虽然有 com.apple.security.cs.allow-dyld-environment-variables = true（允许 DYLD_INSERT_LIBRARIES 到达 dyld），但 Hardened Runtime 默认开启 library validation：异 TeamID 的 dylib 会被拒绝加载。实测注入无效，放弃"零修改运行时注入"路线。

## 4. 最小化补丁方案（19 字节）

综合上面的分析，只需补两个函数：

| 补丁点 | 语义 | 必要性 |
|---|---|---|
| -[LicenseManager hasLicense] → 恒返回 YES | 切断所有本地判定（试用闸门、激活面板、JS 查询） | 核心 |
| -[LicenseManager unfillLicense] → 直接返回 | 使服务器心跳复核无法撤销本地状态 | 防反噬 |

### 4.1 定位补丁偏移

通用二进制两个 slice 的布局（otool -f / otool -l）：

x86_64 slice: 文件起始 0x4000,   __TEXT vmaddr 0x100000000
arm64  slice: 文件起始 0x180000, __TEXT vmaddr 0x100000000
文件偏移 = slice起始 + (符号vmaddr - 0x100000000)

| 函数 | slice | vmaddr | 文件偏移 | 原字节 | 补丁字节 |
|---|---|---|---|---|---|
| hasLicense | arm64 | 0x10006ce5c | 0x1ece5c | 00 0c 40 f9 40 00 00 b4 | 20 00 80 52 c0 03 5f d6（mov w0,#1; ret） |
| unfillLicense | arm64 | 0x10006e8a4 | 0x1ee8a4 | f4 4f be a9 | c0 03 5f d6（ret） |
| hasLicense | x86_64 | 0x10007f9b2 | 0x839b2 | 55 48 89 e5 48 8b | b8 01 00 00 00 c3（mov eax,1; ret） |
| unfillLicense | x86_64 | 0x1000818f5 | 0x858f5 | 55 | c3（ret） |

⚠️ 以上偏移仅适用于 1.10.8 (build 7385)。其他版本需按 §2 的方法重新定位符号地址。

### 4.2 补丁脚本

```
#!/usr/bin/env python3
# patch_typora.py —— Typora 1.10.8 (7385) macOS 激活绕过补丁（教学用途）
path = "/Applications/Typora.app/Contents/MacOS/Typora"
data = bytearray(open(path, 'rb').read())

# (名称, 文件偏移, 期望原字节, 补丁字节)
patches = [
    ("arm64  hasLicense",    0x1ece5c, bytes.fromhex("000c40f9400000b4"),
     bytes.fromhex("20008052c0035fd6")),   # mov w0,#1; ret
    ("arm64  unfillLicense", 0x1ee8a4, bytes.fromhex("f44fbea9"),
     bytes.fromhex("c0035fd6")),           # ret
    ("x86_64 hasLicense",    0x0839b2, bytes.fromhex("554889e5488b"),
     bytes.fromhex("b801000000c3")),       # mov eax,1; ret
    ("x86_64 unfillLicense", 0x0858f5, bytes.fromhex("55"),
     bytes.fromhex("c3")),                 # ret
]
for name, off, orig, patch in patches:
    cur = bytes(data[off:off+len(orig)])
    assert cur == orig, f"{name}: 原字节不匹配！版本可能不对。got {cur.hex()}"
    data[off:off+len(patch)] = patch
    print(f"[OK] {name} @0x{off:x}: {orig.hex()} -> {patch.hex()}")
open(path, 'wb').write(data)
```

先备份再执行：

pkill -x Typora
cp /Applications/Typora.app/Contents/MacOS/Typora /tmp/Typora.bak
python3 patch_typora.py

### 4.3 重新签名

修改二进制后原签名失效，arm64 上无有效签名无法运行，做 ad-hoc 重签：

```
codesign --force --sign - --timestamp=none /Applications/Typora.app
codesign --verify --verbose=2 /Applications/Typora.app
# => valid on disk / satisfies its Designated Requirement
```
